export enum QuoteKind {
  Fact = 'Fact',
  Opinion = 'Opinion'
}

export enum IssueStatus {
  Open = 'Open',
  Closed = 'Closed',
  InProgress = 'InProgress'
}

export enum IssuePriority {
  Low = 'Low',
  Medium = 'Medium',
  High = 'High',
  Critical = 'Critical'
}
